/main.js
/data.json
/node_modules
/coverage
# The plugin folder's runtime lua/: written on load from the filters embedded in
# main.js, and written into by the store. The sources are in lua-filters/bundled.
/lua/
# The wasm builds downloaded into the plugin folder on request — pandoc's 56 MB
# and typst's 27 MB with its fonts, all someone else's releases. The leading
# slash matters — src/wasm/ is ours.
/wasm/
/.env.local
/.idea
