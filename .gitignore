/main.js
/data.json
/node_modules
/coverage
# The plugin folder's runtime lua/: written on load from the filters embedded in
# main.js, and written into by the store. The sources are in lua-filters/bundled.
/lua/
# Pandoc's wasm build, downloaded into the plugin folder on request: 56 MB of
# someone else's release. The leading slash matters — src/wasm/ is ours.
/wasm/
/.env.local
/.idea
