# `vite build` sets NODE_ENV=production whatever the mode, and solid picks its
# runtime by that: without this line `npm run dev` bundles the production build,
# losing the reactivity warnings and the devtools hook. Only read for `-m
# development`, so the release build is unaffected.
NODE_ENV=development
